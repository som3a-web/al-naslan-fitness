# ===========================================================================
# NFC — n8n automation webhooks (optional).
# Leave blank to run the demo with the built-in local AI brain & mock lead flow.
# Set these to your n8n production webhook URLs to go live.
# ===========================================================================

# Website AI Assistant -> n8n (LLM + live availability + CRM)
N8N_CHAT_WEBHOOK_URL=

# Lead capture (free trial / consultation / booking) -> n8n (CRM + WhatsApp confirm)
N8N_LEAD_WEBHOOK_URL=
